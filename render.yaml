services:
  - type: web
    name: school-index-reports
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python school_dashboard.py --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0